{
	"allowedScopes": [
		"environment"
	],
	"description": "Stored metric queries allow you to use a metric key as an alias for common metric selector queries, similar to a database view. These metric keys can then be used like normal metric keys and the system automatically replaces the key with the provided query.",
	"displayName": "Stored metric queries",
	"documentation": "",
	"dynatrace": "1",
	"enums": {},
	"maxObjects": 1,
	"multiObject": false,
	"properties": {
		"pseudo": {
			"default": false,
			"description": "pseudoElement",
			"displayName": "pseudoElement",
			"documentation": "",
			"maxObjects": 1,
			"modificationPolicy": "DEFAULT",
			"nullable": false,
			"type": "boolean"
		}
	},
	"schemaGroups": [
		"group:metrics"
	],
	"schemaId": "builtin:stored-metric-query",
	"types": {},
	"version": "1.0.1"
}